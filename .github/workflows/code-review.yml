name: Code Review

# Automated Claude code review on new pull requests.
# Authenticated with a Claude Pro/Max subscription via an OAuth token
# (repo secret CLAUDE_CODE_OAUTH_TOKEN, generated locally with `claude setup-token`).
#
# To re-review on every push as well, add `synchronize` (and optionally
# `reopened`) to the `types` list below.
on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          plugin_marketplaces: "https://github.com/anthropics/claude-code.git"
          plugins: "code-review@claude-code-plugins"
          prompt: "/code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}"
